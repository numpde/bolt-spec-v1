Local static assets for the browser app.

Keep this directory for things that should ship with the app without a build
step, for example:

- a small favicon
- local screenshots or overlays
- vendored browser libraries
- local fonts
